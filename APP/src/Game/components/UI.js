import React from 'react';
import CanvasOverlay from './UI/CanvasOverlay';
import Sidepanel from './UI/Sidepanel';

const UI = ({ user }) => (
  <>
    <CanvasOverlay />
    <Sidepanel />
  </>
);

export default UI;
